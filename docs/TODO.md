# TODO

- **Build the website** — framework choice (static site generator vs
  hand-rolled), landing page, per-app pages, GitHub Pages deploy. Blocked on:
  ~~final icon system in `brand/`~~ DONE 2026-08-11. See
  `docs/mission.md` for the v1 acceptance shape.
- **Custom domain** — decide whether/when to buy one; Pages supports adding it
  later with no restructuring.
- **Install new icons into app repos** — once `brand/` icons are final, small
  follow-up PRs to FIRE-Biz-iOS, FIRE-Mileage-iOS (PATCH bumps per
  APP-VERSIONING.md), and the RE/Finance apps as they exist.
- **App Store listing names** — decide the descriptive suffix per app
  (e.g. "FIRE Biz — Business Expenses"); home-screen display names are locked
  (FIRE Finance · FIRE Mileage · FIRE Realty · FIRE Biz).
